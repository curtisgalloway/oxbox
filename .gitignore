logs/
.oxbox-review/
# The pre-rename spelling, kept so batch directories written by an older
# checkout stay ignored instead of turning up as untracked files.
.ox-review/
sandbox/
.env
__pycache__/
*.pyc
.venv/

# Rust build output
/target/

# release-train host facts: roles in RELEASE-TRAIN.md, machines here.
RELEASE-TRAIN.local.md
